install sendmail/postfix/ssmtp package

sudo apt-get install libdevice-serialport-perl
sudo apt-get install libmime-lite-perl

export AALARM_PATH_FOLDER_CONFIG="/path/to/aAlarm/sh"
cp config.sh.dist config.sh and edit

cp aalarm.conf.dist aalarm.conf and edit


after package

ssmtp

config mysql
config db django
config db pl

config motion

config aalarm